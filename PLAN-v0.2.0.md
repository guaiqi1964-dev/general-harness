# General Harness v0.2.0 — Agent 自主执行系统命令

> 当前状态：**已实现**（Agent 命令执行与循环编排自 v0.2.0 起随正式发布可用；本文件保留作为设计说明）

## 目标

让 General Harness 具备「Agent」能力：AI 模型经网关可在安全约束下自主执行宿主系统命令，并基于命令输出继续决策，形成 Agent 循环。

## 范围界定

- 引擎（Go）作为命令执行器，模型通过网关请求执行命令并取回结果。
- 提供标准 HTTP API 供 Agent 循环调用；CLI / GUI 可交互确认。
- 命令执行默认关闭，需在 config.yaml 显式配置开启。

## 架构方案

1. **命令执行模块**（引擎内，纯标准库 os/exec）
   - 超时限制、输出大小上限、工作目录限制、环境变量隔离。
2. **安全模型**
   - 命令白名单（允许的路径 / 前缀）、参数黑名单（危险模式）。
   - 可选显式确认开关（CLI / GUI 提示用户批准后执行）。
3. **Agent 循环编排**
   - 模型决策 → 引擎执行 → 结果回传 → 模型继续，直到完成或步数上限。
4. **HTTP API**
   - 规划 POST /v1/agent/run（单条命令）与 /v1/agent/loop（多步循环）。

## 分段任务（后续逐步执行）

1. 安全模型 + 命令执行器实现（含 Windows 进程限制）
2. HTTP API 端点 + Agent 循环编排
3. CLI / GUI 集成与确认交互
4. 测试：危险命令拦截、超时、命令注入、输出截断
5. 文档更新 + 发布 v0.2.0

## 环境

- Go 1.26.6（os/exec 标准库，零新增依赖）
- 构建 / 发布链：Inno Setup 6.7.3、UPX 5.2.0、git 2.54.0（均已就绪）
- 沙箱方案待评估（Windows Job Objects / 受限目录 / 可选确认）

## 待确认问题

- 默认安全策略：白名单模式，还是「默认允许 + 确认」？
- 是否需要持久化 Agent 会话历史？
- 命令输出大小与步数上限的默认值。
